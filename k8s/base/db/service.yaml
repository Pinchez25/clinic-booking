apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: clinic-booking
  labels:
    app.kubernetes.io/name: clinic-booking
    app.kubernetes.io/component: db
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: clinic-booking
    app.kubernetes.io/component: db
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
