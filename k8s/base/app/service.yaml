apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: clinic-booking
  labels:
    app.kubernetes.io/name: clinic-booking
    app.kubernetes.io/component: app
spec:
  selector:
    app.kubernetes.io/name: clinic-booking
    app.kubernetes.io/component: app
  ports:
    - port: 8000
      targetPort: 8000
      name: http
