ALLOWED_HOSTS=127.0.0.1,localhost
SECRET_KEY="v&@q1yg+u-$bzh$*2s9=c&-u=jty@7z2e+5vz)lqr&qexx+csq"
DB_NAME=clinic
DB_USER=clinic
DB_PASSWORD=clinic
DB_HOST=localhost
DB_PORT=5432
# RS256 keys (production only — dev uses HS256 with SECRET_KEY)
# Generate with: openssl genrsa -out private.pem 2048
# Public key:    openssl rsa -in private.pem -pubout -out public.pem
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
CORS_ALLOWED_ORIGINS=http://localhost:3000
DJANGO_SETTINGS_MODULE=clinic.settings.development
